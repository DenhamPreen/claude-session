# cproj

A terminal replacement for the Claude Code desktop app's project sidebar.

`cproj` reads your local Claude Code session history (`~/.claude/projects/*/*.jsonl`),
groups sessions by recency (**Today / Yesterday / This week / Older**), and lets
you fuzzy-pick one with `fzf`. Selecting a row `cd`s into that project and runs
`claude --resume <session>`.

```
Today      14:32  myapp/api                         Add JWT refresh token endpoint
Today      11:08  myapp/web                         Fix dark mode flicker on initial load
Yesterday  16:45  scripts/deploy                    Migrate CI from CircleCI to GH Actions
This week  09:12  notes/research                    Compare vector DB options for RAG
Older      18:03  dotfiles                          Tidy up zsh prompt and aliases
```

## Requirements

- `bash` (3.2+; works with macOS stock bash)
- [`fzf`](https://github.com/junegunn/fzf)
- [`jq`](https://jqlang.github.io/jq/)
- Claude Code CLI (`claude`) installed and on `PATH`

Works on macOS and Linux.

## Install

```sh
git clone https://github.com/denhampreen/cproj.git
cd cproj
./install.sh
```

This symlinks `bin/cproj` into `~/.local/bin/cproj`. Override the target with
`CPROJ_INSTALL_DIR=/usr/local/bin ./install.sh`.

### Homebrew / manual

`cproj` is a single bash script. You can also just copy `bin/cproj` anywhere on
your `PATH` and `chmod +x` it.

## Usage

```
cproj                 # open the picker
cproj envio           # only show sessions whose project path contains "envio"
cproj -y              # resume with --dangerously-skip-permissions
cproj -T              # skip AI titles for this run (use raw first message)
cproj -y indexer      # combine flags with a filter
cproj --help
```

### AI-generated titles

The first user message of a session is often vague ("hey", "ok continue", a pasted
URL). `cproj` can replace the title column with a 4–7 word AI-generated summary.

**Setup:** nothing. Titles are generated by invoking your local `claude` CLI in
headless, non-persistent mode — it piggybacks on your existing Claude
subscription auth. No API key, no extra config.

- **Lazy mode (default):** on each `cproj` run, any uncached session spawns a
  background title generation. This run shows the raw first-message fallback;
  the next run shows the generated title. No blocking, no setup.
- **Eager mode (opt-in):** run `./install-hook.sh` once. This registers a Claude
  Code `Stop` hook that refreshes each session's title at the end of every turn,
  so titles stay current as sessions evolve.

Titles are cached at `${XDG_CACHE_HOME:-~/.cache}/cproj/titles.tsv` and keyed by
session ID + user-message count, so titles are refreshed as sessions grow (not
regenerated on every turn).

Generation uses `haiku` (cheap, fast) by default. Override with
`CPROJ_TITLE_MODEL=sonnet` etc.

### Persistent options

| Env var                         | Effect                                                 |
|---------------------------------|--------------------------------------------------------|
| `CPROJ_SKIP_PERMISSIONS=1`      | Always resume with `--dangerously-skip-permissions`    |
| `CPROJ_CLAUDE_BIN=/path/claude` | Override the `claude` binary                           |
| `CPROJ_PROJECTS_DIR=~/...`      | Override `~/.claude/projects`                          |
| `CPROJ_TITLE_MODEL`             | Title model alias (default `haiku`)                    |
| `CPROJ_CACHE_DIR`               | Title cache dir (default `$XDG_CACHE_HOME/cproj`)      |

Add to `~/.zshrc` / `~/.bashrc` if you want these as defaults.

## How it works

Each Claude Code session is stored as a JSONL file at
`~/.claude/projects/<encoded-cwd>/<session-uuid>.jsonl`. `cproj`:

1. Scans every `*.jsonl`.
2. Extracts the first user message (as the title), the last user-message
   timestamp (for sort order), and the `cwd` / `sessionId` from the file.
3. Filters out sidechain messages and system-injected prompts so titles are
   real user input.
4. Groups and formats rows; pipes them to `fzf`.
5. On selection, `cd`s into the session's original `cwd` and
   `exec`s `claude --resume <id>`.

Because the script `cd`s inside a subprocess, your parent shell's working
directory is unaffected when Claude exits.

## Why

Claude Code's terminal UI doesn't support custom sidebars, so the desktop
app's "recent projects" column has no equivalent in the TUI. `cproj` is the
closest shell-level approximation: one keystroke to jump between past
sessions across every project.

## License

MIT — see [LICENSE](LICENSE).
